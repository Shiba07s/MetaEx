spring:
  datasource:
    url: jdbc:mysql://localhost:3306/micro_users
    username: root
    password: Pupi@12345
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081

